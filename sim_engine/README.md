# sim_engine

Simulation engine for backtesting and paper-trading strategies.

**Status:** Placeholder — will be populated in Phase 4 (Simulation Layer).

When filled:
- Strategy replay against historical Polymarket data
- Uses `StrategyPlugin` interface from `core/plugin_base.py`
- Runs all safety modules in `mode="simulation"` (no real side-effects)
